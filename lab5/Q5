/*
Implement the following decision table using nested ternary operators.
Assume that the grade point average is within the range 0.0 through 4.0.
*/

#include <stdio.h>
int main(){
	float grade;
	printf("Enter grade point average:");
	scanf("%f", &grade);
	grade<1?printf("Failed semester-registration suspended"):grade<2?printf("On probaion for next semester"):grade<3?printf("No message"): grade<3.5?printf("Dean's list for semester"): printf("Highest honors for the semester");
	return 0;
}
